#pragma once
// Copy this file to secrets.h and fill in your own credentials.
// secrets.h is gitignored — never commit real keys.

// WiFi (only needed if ENABLE_WIFI_DEBUG=1)
#define SECRET_WIFI_SSID  "your-ssid"
#define SECRET_WIFI_PASS  "your-password"

// LoRaWAN OTAA credentials — TTN console
// AppEUI / JoinEUI (MSB)
#define SECRET_JOIN_EUI  0x0000000000000000ULL

// DevEUI (MSB)
#define SECRET_DEV_EUI   0x0000000000000000ULL

// AppKey (MSB) — LoRaWAN 1.0.x
static const uint8_t SECRET_APP_KEY[16] = {
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00,
    0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00
};
